language: python
python:
  # We don't actually use the Travis Python, but this keeps it organized.
  - "3.5"

env:
  global:
    - GIT_DESCRIBE_NUMBER=$TRAVIS_BUILD_NUMBER
    - GIT_BUILD_STR=$TRAVIS_COMMIT
  matrix:
    - PYTHON_TARGET=2.7
    - PYTHON_TARGET=3.5

install:
  - sudo apt-get update
  - sudo apt-get -y remove swig
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
  - bash miniconda.sh -b -p "$HOME/miniconda"
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda config --add channels estnltk
  - conda config --set anaconda_upload True
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a

  - conda install conda-build anaconda-client
  - conda 
  - anaconda login --hostname "$TRAVIS_BUILD_NUMBER"_"$PYTHON_TARGET" --username estnltk --password "$ESTNLTK_ANACONDA_PASSWORD"
    
  - conda build --py "$PYTHON_TARGET" conda-recipe #implicitly uploads to anaconda.org/estnltk.

  - anaconda logout #Otherwise consecutive builds might fail.

script:
  echo 'imports were run by conda, full tests out of scope right now'
